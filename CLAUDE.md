# LawRAG —— 面向法律法规领域的检索增强生成系统

简历项目一 + 毕业设计主体。独立仓库，与学习仓库 `D:\Go work` 解耦。

> 📖 每次会话自动加载，保持 **<8KB**。详细内容按需读：`docs/PROJECT-MEMORY.md`（背景）、`docs/adr/`（决策）、`docs/面试弹药库.md`（数据出处）

---

## 一、用户背景（精简）

- 软件工程**准大四**（民办三本），目标 **2026 年 12 月**找 AI 应用开发实习
- 编程基础薄弱：会基本语法和环境配置，但生疏，**无法独立完成项目**
- 每天投入 **4-5 小时**（开学后），英语借助翻译工具可读英文文档
- 有 NVIDIA 显卡，可本地跑模型

### 已掌握（2026-09-11 时点）

学完 LLM API / 多轮 / 流式 / FC / Prompt / Token 管理，RAG 全链路（手写 + LangChain 两版），Agent（手写 ReAct + LangGraph + 工具契约）。**全是散装实验脚本，无工程化经验。**

### 薄弱环节（教学时要注意）

| 环节 | 表现 | 应对 |
|------|------|------|
| **能讲** | 概念理解简单，但**讲不出来**，需要多复述 | 每个模块学完让用户用自己的话复述 |
| **能写** | 手写代码慢，**拼写笔误多** | 检查拼写/缩进/变量名，但不代写 |
| 边界分析 | 想不全 | 主动追问"10 万文档会怎样"这类问题 |
| 常见笔误 | `Choram`/`Ebeddings`/`ciity` 这类拼写、缩进层级、变量名不一致 | review 时重点看 |

---

## 二、教学与协作约定 ★

### 分工线（用户 2026-09-11 确认）

> **面试官会问的地方，用户写。不会被问的地方，助手给。**

| 用户手写（助手给骨架+留空+review） | 助手给完整代码（用户读+改） |
|---|---|
| 混合检索、RRF、rerank、阈值、token 裁剪 | Dockerfile / docker-compose |
| 法条结构化解析 + 分块 | Alembic 迁移脚本 |
| 评估指标计算（Recall/MRR/Hit/Citation F1） | Pydantic schema、ORM 模型定义 |
| 引用三层校验器 | FastAPI 路由骨架、config、日志配置 |
| Agentic RAG 的图 + 工具 + 护栏 | Streamlit 重复部分 |
| trace 计量逻辑 | 测试脚手架 |

### 代码引导三级（铁律）

- **【提示】**：文字/伪代码说清"这一步做哪几件事"——已见过的概念，不给代码
- **【骨架】**：给框架 + 关键行留空——概念懂但第一次写
- **【示例】**：**新概念**的最小可跑示例（详细注释），只给"看懂长什么样"，**不含当天实验答案**
- 同一概念**只给一次【示例】**，之后全靠【提示】/【骨架】
- **用户写、助手 review、绝不代写核心链路**

### 用户明确要求的底线（2026-09-11）

> "核心一定要会，而且**经得住问，从为什么这样设计开始**。"

所以每个模块必须：**先讲为什么这样设计 → 再动手 → 再写 ADR → 再拷问**。不许跳步直接写代码。

### 其他规矩

- 每个里程碑开始前出**任务单**（目标/前置/设计讨论/要写的代码/助手给的部分/验收标准/ADR条目/拷问点）
- 每个关键决策写一条 **ADR**（`docs/adr/`，格式见该目录 README）
- 完成后更新 `docs/面试弹药库.md`（数字、参数、出处）
- 诚实报告：测试失败就说失败，跳过步骤就说跳过

---

## 三、环境

| 项 | 值 |
|----|-----|
| Python | `D:\LawRAG\venv\Scripts\python.exe`（从 gowork 复制，**不要用系统 python**） |
| Ollama | `OLLAMA_MODELS=D:\Ollama\models`，服务必须带此环境变量启动 |
| 模型 | LLM `qwen2.5:7b` / Embedding `bge-m3`（1024 维） |
| Rerank | 云端硅基流动 `BAAI/bge-reranker-v2-m3`（抽象成接口，可切本地） |
| 数据库 | MySQL 跑在 Docker，宿主机端口 **3307**（3306 被本机原有 MySQL 占用），容器已起。重启：`cd docker && docker compose up -d mysql` |
| 向量库 | ChromaDB（抽象成接口，可切 Qdrant） |

**已知环境问题（非代码错）**：GBK 控制台打印中文/emoji 会 `UnicodeEncodeError`。

---

## 四、铁律

1. **`.env` 绝不提交**（含 API Key，泄露即失效+安全问题）。提交前 `git status` 扫一眼
2. **`chroma_data*/` 不进 git**（体积大，可重建）
3. **法条原文是公有领域**（《著作权法》第五条），可以入 git；但"法条释义/案例评析"有版权，不碰
4. **分层依赖单向**：`api → services → core → models`，用 `import-linter` 强制，违规报错
5. **向量库是索引，不是真相源**——`chunks` 表才是

---

## 五、里程碑进度

**方案 A**：前 4 周完成 M0-M5 + M7（能演示 + 能写论文），第 5 周补 M6 + M8。

| 里程碑 | 内容 | 天 | 状态 |
|--------|------|-----|------|
| **M0** | 地基：仓库结构 / config / 统一异常 / LLM+Reranker Provider 抽象 / 健康检查 | 2 | 🔄 设计讨论 1/5 |
| **M1** | 9 张表 ORM + Alembic + 知识库 CRUD | 2 | ⬜ |
| **M2** | ★ 法条结构化解析 + 接入链路（一条一 chunk、时效字段、任务状态机、幂等） | 4 | ⬜ |
| **M3** | ★ 检索层（VectorStore 抽象 + 混合检索 + RRF + rerank + 阈值 + token 预算 + 条款号精确查询 + 时间过滤） | 4 | ⬜ |
| **M4** | ★ 问答链路（条款级引用 + 引用三层校验器 + 防幻觉 + 拒答 + 版本声明 + SSE + trace） | 3 | ⬜ |
| **M5** | ★★ 评估体系（Recall@5 / MRR / Hit@1 / **Citation F1** + 一键回归 + 报告） | 2 | ⬜ |
| **M7** | Streamlit 前端（知识库 / 文档 / 问答与引用高亮 / 评估看板） | 2 | ⬜ |
| **M6** | ★★ Agentic RAG（query 改写 + 反思节点 + 检索工具化 + 护栏） | 3 | ⬜ |
| **M8** | 交付（Docker Compose + README + 架构图 + 测试 + 论文素材） | 2 | ⬜ |

**进度落后时的砍法**：M8 测试 → M7 前端深度 → M6 反思节点。**M5 评估体系绝不砍**（论文实验章全靠它）。

---

## 六、决策索引（详见 `docs/adr/`）

| # | 决策 | 一句话理由 |
|---|------|-----------|
| D1 | 领域 = 法律法规（先聚焦劳动法） | 语料公开；**条文号是天然评估金标准** |
| D2 | 独立仓库 `D:\LawRAG` | 与学习仓库解耦 |
| D3 | FastAPI + Streamlit | 答辩要演示，论文要架构图 |
| D4 | 全本地 Chroma + MySQL + Ollama | 零成本、可离线、答辩不翻车 |
| D5 | rerank 用云端 API + 抽象接口 | 本地跑 reranker 太重 |
| D6 | 时间方案 A（4 周 + 第 5 周） | 前 4 周锁定能演示能写论文 |
| D7 | 核心链路用户手写，样板代码助手给 | 被问的地方自己会 |
| D8 | 毕设主体 = 项目一，Agent 作模块 | RAG 边界清晰、有量化指标 |
| D9 | **一条法条 = 一个 chunk**，保留五级层级 | 法条是天然语义单元，滑窗切会产生无指代碎片 |
| D10 | 语料用现成资源，**不自己写爬虫** | 精力花在结构化+时效性+引用校验 |
| D11 | 核心差异化 = **时效性 + 条款级引用校验** | 论文实证：条款号走 SQL 精确匹配，引用 F1 0.897 vs BM25 0.718 |

---

## 七、真正交付物：17 条"经得住拷问"规格表

见 `docs/面试弹药库.md`。**接不住拷问的功能，不做。**

1-10 通用（评估 / 混合检索理由 / rerank 位置 / 10万文档 / 文档更新 / 多租户隔离 / 防幻觉 / LLM 降级 / 成本计量 / Agent 死循环）· 11-17 法律特有（时效性 / **款级部分失效** / **条款号精确查询** / 引用三层校验 / 新旧法冲突 / 免责拒答 / 同义词）

---

## 八、下一步

**当前：M0 地基 —— 设计讨论 1/5 完成**（任务单 `docs/任务单/M0-地基.md`）
节奏：**Q1-Q5 讨论通了才写代码**，不许跳步。

- ✅ **Q1 为什么分层** —— 用户答对：评估脚本没有 HTTP 服务器，不分层则 core 没法复用
- ⬜ Q2 Provider 为什么抽象（"看起来过度设计，其实不是"）
- ⬜ Q3 统一异常的意义　⬜ Q4 健康检查检查什么　⬜ Q5 core/ 不许 import FastAPI

**环境就绪**：远程仓库已推送 `xjx521/LawRAG`（`main`）· MySQL 容器已起（3307，healthy）。
